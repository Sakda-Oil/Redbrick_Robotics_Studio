# Redbrick Arduino extension modules

Future phases add isolated modules under this directory for `cli`, `projects`, `boards`, `ports`, `build`, `libraries`, `serial`, and `language`. Phase 1 intentionally contains only extension activation, Arduino sketch language registration, configuration, and standard Arduino path discovery.
